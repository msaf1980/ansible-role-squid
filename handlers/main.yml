---
# handlers file for squid

- name: restart squid
  service:
    name: "{{ squid_service_name }}"
    state: restarted
  tags:
    - squid

- name: reload squid
  service:
    name: "{{ squid_service_name }}"
    state: reloaded
  tags:
    - squid
